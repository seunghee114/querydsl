spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:tcp://localhost/~/h2DB/querydsl
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true   # sql?? system ???
        format_sql: true
logging.level:
  org.hibernate.SQL: debug  # sql?? logger?
#  org.hibernate.type : trace  # sql?? ???? ?? ?? ? ? ??